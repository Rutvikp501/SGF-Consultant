<main>
	<%- include('../partials/agentSidebar') %>

		<div id="main-wrapper">

			<!-- <div class="bg-white shadow-sm p-3">
			<span class="me-3" id="sidebar-toggler-btn"><i class="fas fa-bars"></i></span>
			<h5 class="m-0 color-theme d-inline-block">Dashboard</h5>
		</div> -->
		<div class="cards">
			<div class="card red">
				<p class="tip">numAllLeads</p>
				<p class="second-text"><%= numAllLeads %></p>
			</div>
			<div class="card green">
				<p class="tip">Converted Leads</p>
				<p class="second-text"><%= numConvertedLeads %></p>
			</div>
			<div class="card yellow">
				<p class="tip">Pending Leads</p>
				<p class="second-text"><%= numPendingLeads %></p>
			</div>
			<div class="card red">
				<p class="tip">Junk Leads</p>
				<p class="second-text"><%= numJunkLeads %></p>
			</div>
		</div>
			


		</div>
</main>
<script type="text/javascript">
	// Load google charts
	google.charts.load('current', {'packages':['corechart']});
	google.charts.setOnLoadCallback(drawChart);
	
	// Draw the chart and set the chart values
	function drawChart() {
	  var data = google.visualization.arrayToDataTable([
	  ['Task', 'Hours per Day'],
	  ['Work', 8],
	  ['Friends', 2],
	  ['Eat', 2],
	  ['TV', 2],
	  ['Gym', 2],
	  ['Sleep', 8]
	]);
	
	  // Optional; add a title and set the width and height of the chart
	  var options = {'title':'My Average Day', 'width':550, 'height':400};
	
	  // Display the chart inside the <div> element with id="piechart"
	  var chart = new google.visualization.PieChart(document.getElementById('piechart'));
	  chart.draw(data, options);
	}
	</script>